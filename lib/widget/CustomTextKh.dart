import 'package:flutter/material.dart';
class TextKh extends StatelessWidget {
  const TextKh({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}